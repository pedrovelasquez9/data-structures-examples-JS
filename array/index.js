// Definition
const myArray = ["a", "b", "c", "d"];

// Insert new value in array
myArray.push("e");
console.log(myArray);

// get value in position 2
const arrayValue = myArray[2];
console.log(arrayValue);

// Iterate over the array
for (let i = 0; i < myArray.length; i++) {
    console.log(myArray[i]);
}